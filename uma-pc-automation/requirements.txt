# PC bot dependencies — install with: pip install -r requirements.txt

# Testing (dev only)
pytest>=8.0

# Screen capture (Windows — DXGI GPU duplication)
# dxcam>=0.0.5

# Input injection (DirectInput-level, game-safe)
# pydirectinput>=1.0.4
# pywin32>=306

# Computer vision
# opencv-python>=4.9
# Pillow>=10.0

# OCR — stat digit reading
# easyocr>=1.7
